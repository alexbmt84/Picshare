// Lozad scripts for image lazy loading...

document.addEventListener("DOMContentLoaded", function () {

    // Initialize lozad
    const observer = lozad(); // You can also pass configuration options to lozad if needed
    observer.observe();

});
